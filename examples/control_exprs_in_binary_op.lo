enum Side { Left | Right }

main {
    let num: int = if false 5 else 1;
    out((5 + num));
    out((5 + (if true 5 else 10)));

    let side: Side = #Right;
    out((match side { #Left -> "Hello" #Right -> 10 } ~ " world!"))
}
