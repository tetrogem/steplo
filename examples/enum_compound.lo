enum Shape { Square | Circle | Triangle }

type ShapeInfo = { shape: Shape, area: num, perimeter: num };

main {
    let shapes: [Shape; 3] = [#Triangle, #Square, #Circle];
    let more_shapes: [Shape; 10] = [#Circle, #Circle, #Triangle...];
    let info: ShapeInfo = {
        shape: #Square,
        area: 4,
        perimeter: 8,
    };
}
