enum Foo { A | B | C }

main {
    let foo: Foo = Foo#A;
    let uint: uint = <<uint>>foo;
    let val: val = <<val>>foo;
    let any: any = foo;

    let bool: bool = (foo == foo);
    // bool = (foo == uint);
    bool = (foo > foo);
    // bool = (foo < uint);
    // bool = (foo == any);
    // bool = (foo == bool);
}

// No output
