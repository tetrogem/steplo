enum Foo { A | B | C }

main |foo: Foo, uint: uint, val: val, any: any, bool: bool| {
    foo = Foo#A;
    uint = <<uint>>foo;
    val = <<val>>foo;
    any = foo;

    bool = (foo == foo);
    // bool = (foo == uint);
    bool = (foo > foo);
    // bool = (foo < uint);
    // bool = (foo == any);
    // bool = (foo == bool);
}
