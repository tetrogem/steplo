main |i: uint| {
    i = 20;
    iterate(&i);
}

func iterate(i_r: &uint) |i: uint| {
    i = *i_r;

    if (0 != i) {
        *i_r = <uint>(i - 1);
        greet();
        iterate(i_r);
    }
}

func greet() || {
    out("Hello!");
}
