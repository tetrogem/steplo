main {
    let i: uint = 20;
    iterate(&i);
}

func iterate(i_r: &uint) {
    let i: uint = *i_r;

    if (i != 0) {
        *i_r = <<uint>>(i - 1);
        greet();
        iterate(i_r);
    }
}

func greet() {
    out("Hello!");
}
