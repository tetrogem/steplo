main {
    let num: num = num_random(-10, 20);
    let int: int = int_random(-10, 20);
    let uint: uint = uint_random(10, 20);

    out(num);
    out(int);
    out(uint);
}

// Expected stdout: Random
