main {
    let a: str = "Hello!";
    let b: &str = &a;
    let c: &&str = &b;
    in(b);

    let val: val = "Hello world!";
    val = 0;
    let num: num = 10.5;
    num = -4.7;
    num = 0;
    let int: int = -32;
    int = 0;
    let uint: uint = 88;
    uint = 0;
    let bool: bool = true;
    bool = false;

    uint = (uint + uint);
    int = (uint + uint);
    // uint = (int + int);
    int = (uint + int);
}
