main {
    out("Enter how many iterations to do:");

    let i: int = <<int>>in();

    out("Start!");

    while (i > 0) {
        out(i);
        i = (i - 1);
    }

    out("End!");
}

// Interactive
