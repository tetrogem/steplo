main |a: num, b: num, c: num, sum: num| {
    a = 10;
    b = 15;
    c = 50;

    sum = (a + b);
    out(sum);

    sum = (b + c);
    out(sum);

    sum = (100 + 200);
    out(sum);

    out((500 + 75));
}
