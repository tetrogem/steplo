main |a, msg| {
    msg = lit "Starting...";
    out(msg);

    a = lit "1";
    if a {
        foo();
    }

    msg = lit "Stopping...";
    out(msg);
}

func foo() |msg| {
    msg = lit "Foo was called!";
    out(msg);
}
