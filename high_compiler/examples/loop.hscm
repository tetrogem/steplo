main |i| {
    i = "20";
    iterate(ref i);
}

func iterate(i_ref) |i| {
    i = i_ref | deref;

    if "0" | eq i | not {
        deref i_ref = i | sub "1";
        greet();
        iterate(i_ref);
    }
}

func greet() || {
    out("Hello!");
}
