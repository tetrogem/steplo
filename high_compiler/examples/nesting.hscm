main || {
    out(lit "Enter main");
    a();
    out(lit "Exit main");
}

func a() || {
    out(lit "Enter a");
    b();
    out(lit "Exit a");
}

func b() || {
    out(lit "Enter b");
    c();
    out(lit "Exit b");
}

func c() || {
    out(lit "Enter c");
    out(lit "Exit c");
}
