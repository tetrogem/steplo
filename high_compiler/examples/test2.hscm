main |msg1, msg2, expr, msg2_ref| {
    msg = lit "hello";
    msg2 = lit "hello";
    msg2_ref = ref msg2;

    expr = lit "second";
    expr = eq msg check;
    second(msg2) if expr;
}

func second(msg2_ref) || {
    ref msg2_ref = lit "world";
}

func other(msg2_ref) || {
    ref msg2_ref = lit "everyone";
}
