main |i, cond| {
    out("Enter how many iterations to do:");
    in(ref i);

    out("Start!");

    cond = 1;
    while cond {
        out(i);
        i = i | sub 1;
        cond = i | neq 0;
    }

    out("End!");
}
