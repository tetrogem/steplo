main |i, zero, one, cond, msg| {
    i = lit "10";
    one = lit "1";
    zero = lit "0";

    out(lit "Start!");

    cond = lit "1";
    while cond {
        out(copy i);
        i = sub i one;
        cond = eq i zero;
        cond = not cond;
    }

    out(lit "End!");
}
